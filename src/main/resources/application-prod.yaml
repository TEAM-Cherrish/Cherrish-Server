spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
    open-in-view: false

logging:
  level:
    root: INFO
    com.sopt.cherrish: INFO
    org.hibernate.SQL: WARN
